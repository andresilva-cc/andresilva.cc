/* Canonical origin for the deployed site. Used wherever absolute
 * URLs are emitted — RSS items, sitemap entries, JSON-LD, OG meta.
 * Centralized so a future origin change is a one-line edit. */
export const SITE_ORIGIN = 'https://andresilva.cc';
